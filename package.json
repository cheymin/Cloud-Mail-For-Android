{
  "name": "netlify-vpn-proxy",
  "version": "1.0.0",
  "description": "VPN转发服务 - 支持Clash链接",
  "scripts": {
    "dev": "netlify dev",
    "build": "netlify build"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.0.0"
  }
}
